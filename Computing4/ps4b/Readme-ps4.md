# PS4: AniPlayer

## Contact
Name: AJ Audet
Section: 204

Partner Name: Hoang Bach Nguyen
Partner Section: 201

Time to Complete: 14 Hours

## Description
Displays an animation based off of given data in a json file. Frames are generated by using keyframes and linear interpolation to smmothly connect the different keyframes

### Features

#### Part a
Describe what your major decisions were and why you did things that way.

AniPlayer: Based off of the given information from a json file, creates shapes, or images to display onto a SFML Window.
Component: Parses through a json file looking for key information such as "shape", "width", "height", "radius", "keyframes", etc.
KeyFrame: Moves a given object by an ammount specified in the "keyframes" section in a json file.

The major design decision was using a static factory function Component::fromJson() to construct the appropriate subclass based on the "shape" field in the JSON. This keeps construction logic in one place so that AniPlayer and CompositeComponent do not need
to know which concrete type they are creating.

Color parsing supports both hex strings (6-digit RGB and 8-digit RGBA, with or without a leading #) and named colors.

CircleComponent and ImageComponent are drawn centered at their KeyFrame position. RectangleComponent uses its own "x" and "y" fields as a local offset applied via setPosition, drawing from the top-left. TextComponent draws from the top-left corner.

Child components in a CompositeComponent are drawn relative to their parent by accumulating transforms through sf::RenderStates. The parent's keyframe transform is applied to states first, then each child's own transform is applied on top, so children are positioned in the parent's local coordinate space.

#### Part b

Tweening is handled by linear interpolation between consecutive keyframes. At any given time, currFrame() finds the two surrounding keyframes and calls KeyFrame::tween() which computes the interpolated position, scale, and rotation using the formula: value = start + (end - start) * (elapsed / duration). Fields are interpolated independently so parameters can all change simultaneously without affecting each other.

Timekeeping uses sf::Clock as a stopwatch that runs continuously while the video plays. The elapsed time from the clock plus a pauseOffset accumulates the total video time. When paused, pauseOffset saves the current time and the clock is left running but ignored. When unpaused, the clock continues and pauseOffset + clock.getElapsedTime() resumes from exactly where it stopped. Pressing R resets both the clock and pauseOffset to zero, restarting the video from the beginning. After the final keyframe, the video freezes on the last frame since currFrame() clamps to keyFrames_.back() for any time beyond the last keyframe.

The AniPlayer tween function checks if there is a scene to be displayed, if there is then the Component tweening function is called on all the Components stored in a given scene at a given time that is tracked by an internal sf::Clock object.

The Component tween function sets a current time variable to the given time in the function, the real way the frames are selected for tweening is in the currFrame function. currFrame checks if the vector that stores the keyframes is empty, if it is, it will create an empty keyframe with default parameters of 0 for x and 0 for y with 1 for the x and y scale with 0 degrees of rotation for theta. If the current time is the same as the time stored in the first keyframe then that keyframe is returned. If the current time is the same as the time stored in the last keyframe then that last keyframe is returned. Otherwise the keyframes are iterated through, and if the current time is greater than or equal to the keyframe at position i and less than  equal to the keyframe at position i + 1 then the keyframe tween function is called from the frame at position i on the frame at position    i + 1, and the current time. After all those calculations are complete the final frame is returned to end the animation.

The KeyFrame tween function handles all of the math behind lineral interpolation. By using the data stored from the frame that is calling tween and the given frame and time we are able to create a smooth transistion between frames that has no stutters. The duration of the frame is calculated before being used to calculate the elapsed time, then the elapsed time is divided by the duration to find the difference. That difference is then used to calculate how x, y, xScale, yScale, and theta are calculated for the tweened frame.

### Memory

All components are stored on the heap and all smart pointers, specifically they are unique pointers. AniPlayer holds a std::unique_ptr<Component> to store the type of Component being used. CompositeComponent holds a std::vector<std::unique_ptr<Component>> for its children. There are no dangling pointers anywhere in the code, so there are no memory leaks because the pointers are deallocated after the program finishes running.

### Issues
What did you have trouble with?  What did you learn?  What doesn't work?  Be honest.  You might be penalized if you claim something works and it doesn't.

There were issues initially when it came to understanding how JSON files are read and used. Especially when designing the different Component classes we had vauge ideas from the instuctions on how the JSON was supposed to be structured so we could parse through it, but after a while it clicked and we were able to overcome that issue. 

As is the Static Animation Player does not have any errors and eveyrthing works as intended.

When developing the tweening functions for part B there was a little bit of struggle at first due to how the math for calculating the position for the new keyframe would be generated in a smooth manner rather than snaping to position. After figuring out the pattern that the frames needed to follow, it was smooth sailing.

As is the Animation Player does not have any errors and everything works as intended.

### Extra Credit

Keyframe carry-over: missing keyframe fields inherit from the previous keyframe instead of defaulting to 0, patched at load time in Component constructor
Loop animation: components support a "loop", true JSON field that wraps time back to the start of the keyframe range, implemented in Component::tween()
Background music: moved to AniPlayer level with pause/restart support hooked into P and R keys
Named colors: parseColor supports SFML basic colors

## Pair Programming
Working with a partner helped divide the work as one person focused on the components hierarchy and constructors while the other handled JSON parsing and the keyframe transform logic rendering. We gave code review between each others to solve on bugs.

## Acknowledgements
List all sources of help including the instructor or TAs, classmates, and web pages.
If you used images or other resources than the ones provided, list them here.

Kenney Shape Characters (CC0): https://kenney.nl/assets/shape-characters
"Morning Mood" By Edvard Grieg, performed by Musopen Symphony Orchestra (CC-PDM): https://en.wikipedia.org/wiki/Morning_Mood
Roboto font by Christian Robertson (SIL Open Font License): https://fonts.google.com/specimen/Roboto/license
SFML 3.0.2 Documentation Page: https://www.sfml-dev.org/documentation/3.0.2/
Kyoko Kirigiri PNG clipart (Non-Commercial Use, no attribution required): https://www.hiclipart.com/free-transparent-background-png-clipart-bvdqg
"Thick of it Thomas the train remix extended music" by SixtyCargp: https://www.youtube.com/watch?v=NLR3EA3DMPY